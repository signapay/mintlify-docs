---
title: "SAML SSO"
sidebarTitle: "Saml sso"
description: "SP-initiated SAML SSO entry point. If the caller has a valid session, returns an auto-submit HTML form that POSTs the signed SAML Response to the SP. Otherwise, redirects to the CRM login page with th"
openapi: "GET /v1/saml/sso"
---
